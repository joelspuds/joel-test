{% macro link(text='', url=false, back=false, paragraph=false, extraClass='') %}
  {% if text %}
      <a {{ 'href="' + url + '"' if url }} class="govuk-link {{ extraClass }} {{ 'link-back' if back }}" >{{ text }}</a>
  {% endif %}
{% endmacro %}
