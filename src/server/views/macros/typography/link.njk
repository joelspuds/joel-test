{% macro link(text='', url=false, back=false, paragraph=false) %}
  {% if text %}
      <a {{ 'href="' + url + '"' if url }} class="govuk-link" {{ 'class="link-back"' if back }}>{{ text }}</a>
  {% endif %}
{% endmacro %}
