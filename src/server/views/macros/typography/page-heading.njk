{% macro pageHeading(heading='', secondary='', extraClass=false) %}
  {% if heading %}
    <h1 class="govuk-heading-xl {{ extraClass if extraClass }}">
      {% if secondary %}<span class="govuk-heading-secondary">{{ secondary }}</span>{% endif %}
      {{ heading }}
    </h1>
  {% endif %}
{% endmacro %}
